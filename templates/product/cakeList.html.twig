{% extends "base.html.twig" %}
{% block title %}liste des gateaux{% endblock %}
{% block body %}
    <div class="product-list">
        {% for cake in cakeList %}
        <div class="item">
            <a href="{{ path('productGateau', { id: cake.id }) }}">
                <div class="product">
                    <img src="{{ asset(imageFolder ~ cake.getImage()) }}">
                    <h2>{{ cake.title }}</h2>
                    <p>Prix : {{ cake.price }} €</p>
                    <button>ajouter au panier</button>
                </div>
            </a>
        </div>
        {% endfor %}
    </div>
{% endblock %}
