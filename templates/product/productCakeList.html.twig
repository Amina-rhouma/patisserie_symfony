{% extends "base.html.twig" %}
{% block title %}liste des gateaux{% endblock %}
{% block stylesheets %}
    {{ parent() }}

    <link rel="stylesheet" type="text/css" href="{{ asset('css/product-list.css') }}" />
{% endblock %}

{% block body %}
    <div class="product-list">
        {% for cake in cakeList %}
        <div class="item">
            <div class="product">
                <img src="{{ asset(cakeImagesFolder ~ cake.getImage()) }}">
                <h2>{{ cake.title }}</h2>
                <p>{{ cake.price }}€</p>
                <button>ajouter au panier</button>
            </div>
        </div>
        {% endfor %}
    </div>
{% endblock %}
